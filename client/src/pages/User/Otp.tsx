import React from 'react';
import OtpForm from '../../components/user/OtpForm.tsx';
import Layout from '../../components/layout/Layout.tsx';

const Otp = () => {
    return (
        <Layout>
            <OtpForm />
        </Layout>
    );
};

export default Otp;